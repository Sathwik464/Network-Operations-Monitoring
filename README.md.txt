# 🩺 System Health Monitor (Shell Script)

This script monitors system resources such as CPU, Memory, and Disk usage.
It stores logs in the `logs/` directory with timestamps.


```bash
chmod +x health_monitor.sh
./health_monitor.sh
